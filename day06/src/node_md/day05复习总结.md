```
数组相关算法
    1.累积求和，平均值等
    2.查找：顺序查找，二分法查找，求最值以及下标
    3.反转：1.借用新数组 2.不借用数组
    4.扩容：在尾部添加一个数据，需要增加数组的长度
    5.插入：先扩容，再向后移动插入位置后的每个元素，最后载指定位置擦插入元素
    6.删除：删除数组中元素后，向前移动删除为之后的每个元素
    7.排序：冒泡排序，直接排序，其它排序算法

    Arrays工具类 
    Arrays.sort()
    
    各种排序
    快速排序等等
```